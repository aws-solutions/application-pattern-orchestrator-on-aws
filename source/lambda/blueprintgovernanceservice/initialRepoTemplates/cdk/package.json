{
    "private": true,
    "description": "CDK based pattern",
    "license": "Apache-2.0",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "yarn workspaces run build",
        "test": "yarn workspaces run test",
        "synth": "yarn workspace cdk-test-app run synth"
    }
}